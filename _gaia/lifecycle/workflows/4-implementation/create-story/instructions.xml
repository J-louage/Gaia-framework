<workflow name="create-story">
<critical>
  <mandate>Story files must include YAML frontmatter with key, status, dependencies</mandate>
  <mandate>All acceptance criteria MUST use Given/When/Then format: "Given {context}, when {action}, then {expected result}"</mandate>
  <mandate>Check web access availability if story references external APIs</mandate>
</critical>
<step n="1" title="Select Story">
  <action>Read {planning_artifacts}/epics-and-stories.md — parse all stories with their keys, titles, priorities, sizes, risk levels, and dependencies.</action>
  <action>Scan {implementation_artifacts}/ for existing story files matching *-*.md patterns. Record which story keys already have individual files.</action>
  <action>Build a list of stories that do NOT yet have individual story files.</action>
  <action>Sort the list by: Priority (P0 first, then P1, then P2), then dependency topology (stories with no dependencies first).</action>
  <check if="user provided a story key (e.g., /gaia-create-story E1-S2)">
    Use the provided story key directly — skip the selection list. Verify the key exists in epics-and-stories.md. If a story file already exists for this key, warn the user: "Story file for {key} already exists. Proceeding will overwrite it."
  </check>
  <check if="no story key provided">
    Display the prioritized list of stories without files as a table:
    | # | Key | Title | Priority | Size | Risk | Depends On |
    Mark P0 stories with a ⚠ indicator.
    <ask>Which story do you want to create? Select from the list above (provide the story key).</ask>
  </check>
</step>
<step n="2" title="Load Context">
  <action>Read story summary from epics-and-stories.md</action>
  <action>Read architecture.md for technical context</action>
</step>
<step n="3" title="Web Access Guard">
  <check if="story_references_external_apis">Check if web tools available for API documentation lookup</check>
  <check if="no_web_access">Note limitation in story file</check>
</step>
<step n="4" title="Elaborate Story">
  <action>Present a brief summary of what was loaded from epics and architecture context for this story.</action>
  <ask>Are there specific edge cases or error scenarios to call out in the acceptance criteria?</ask>
  <ask>Any implementation preferences or constraints not in the architecture? (libraries, patterns, performance targets)</ask>
  <ask>Should any acceptance criteria be split out or deprioritized?</ask>
  <ask>Any additional context, stakeholder notes, or dependencies to include?</ask>
  <action>Incorporate user responses into the story elaboration</action>
  <action>Expand acceptance criteria in Given/When/Then format: "Given {context}, when {action}, then {expected result}"</action>
  <action>Add technical notes from architecture context</action>
  <action>Define subtasks for implementation</action>
</step>
<step n="5" title="Definition of Done">
  <action>Append DoD checklist: all AC met, tests pass, code reviewed, docs updated</action>
</step>
<step n="6" title="Generate Output">
  <action>Read the story template from {project-root}/_gaia/lifecycle/templates/story-template.md</action>
  <action>Set story status to 'backlog' in the story file header. Sprint assignment will happen later during /gaia-sprint-plan.</action>
  <template-output file="{implementation_artifacts}/{story_key}-{story_title_slug}.md">
    Generate story file following the story-template.md structure. The filename must use the story key and slugified title (e.g., 1.2-user-login.md). Fill all template sections: User Story, Acceptance Criteria, Tasks/Subtasks (linked to AC numbers), Dev Notes, Technical Notes, Dependencies, Test Scenarios, Project Structure Notes, References, Dev Agent Record, and Estimate. Set status to 'backlog'.
  </template-output>
</step>
<next-step source="lifecycle-sequence.yaml">
  <primary command="/gaia-validate-story">Validate the story file for completeness and quality</primary>
  <alternative command="/gaia-create-story" context="To create another story before validating"/>
</next-step>
</workflow>
