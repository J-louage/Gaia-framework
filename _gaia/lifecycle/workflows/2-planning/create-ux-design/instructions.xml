<workflow name="create-ux-design">
<critical>
  <mandate>PRD must exist before starting UX design</mandate>
  <mandate>Every design decision must trace to a user need from the PRD</mandate>
</critical>
<step n="1" title="Load PRD">
  <action>Read {planning_artifacts}/prd.md</action>
  <action>Extract user personas, user journeys, and functional requirements</action>
</step>
<step n="2" title="User Personas">
  <action>Refine persona definitions from PRD</action>
  <action>Add: scenarios, goals, tech proficiency, accessibility needs</action>
</step>
<step n="3" title="Information Architecture">
  <action>Design sitemap and navigation structure</action>
  <action>Define content hierarchy and page relationships</action>
  <action>Map each page or section to the FR IDs it serves — every page must trace to at least one FR. Flag any user-facing FR from the PRD that has no corresponding page in the sitemap.</action>
</step>
<step n="4" title="Wireframes">
  <action>Create text-based wireframe descriptions for key screens</action>
  <action>Define layout, component placement, interaction patterns</action>
  <action>Annotate each wireframe with the FR IDs it addresses. Flag any FR with user-facing behavior that has no wireframe representation.</action>
</step>
<step n="5" title="Interaction Patterns">
  <action>Define common UI patterns used across the application</action>
  <action>Specify component library or design system choices</action>
  <action>Document form behaviors, validation, error states</action>
  <action>Map each interaction flow to the corresponding user journey from the PRD. Every PRD user journey must have a defined interaction pattern.</action>
</step>
<step n="6" title="Accessibility">
  <action>Define WCAG compliance targets (A, AA, AAA)</action>
  <action>Plan keyboard navigation, screen reader support</action>
  <action>Define color contrast and text sizing standards</action>
</step>
<step n="7" title="Figma Import Mode (Optional)" if="figma_mcp_available">
  <ask>A Figma MCP server is available. Would you like to import an existing Figma file to generate ux-design.md? This reads design data from Figma (read-only — no write operations). (import / skip)</ask>
  <action if="import">JIT-load figma-integration:import-detection section. Prompt user for Figma file URL or raw file key. Parse and validate the key via MCP.</action>
  <action if="import">JIT-load figma-integration:import-discovery section. Run page and frame discovery — enumerate all pages and frames from the Figma file.</action>
  <action if="import">JIT-load figma-integration:import-tokens section. Extract design tokens from the Figma file and write design-tokens.json in W3C DTCG format to {planning_artifacts}/design-system/.</action>
  <action if="import">JIT-load figma-integration:import-screens section. Build screen inventory from frame hierarchy with viewport classification and component instance extraction.</action>
  <action if="import">JIT-load figma-integration:import-generate section. Generate ux-design.md with figma: YAML frontmatter (file_key, pages, node_ids), Design Tokens section, Component Inventory, and Screen Descriptions.</action>
  <action if="skip">Figma import skipped. Proceeding with manual UX design generation.</action>
</step>
<step n="8" title="Generate Output">
  <template-output file="{planning_artifacts}/ux-design.md">
    Generate UX design document with: personas, information architecture, wireframe descriptions, interaction patterns, component specifications, accessibility plan, and FR-to-Screen Mapping table (FR ID | Screen/Page | Wireframe Section). If Figma import was used in Step 7, the document is already populated — enhance it with any additional UX decisions from Steps 2-6.
  </template-output>
</step>
<step n="9" title="Optional: Accessibility Review">
  <ask>Would you like to review the UX design for WCAG 2.1 accessibility compliance? This spawns a subagent in a separate context. Recommended for user-facing applications. (yes / skip)</ask>
  <action>If yes: spawn a subagent using the Agent tool: "Load {project-root}/_gaia/core/tasks/review-accessibility.xml. Read its entire contents. Target: {planning_artifacts}/ux-design.md. Follow the flow steps EXACTLY. Generate accessibility findings report."</action>
  <action>If skip: accessibility review can be run anytime later with /gaia-review-a11y</action>
</step>
<next-step source="lifecycle-sequence.yaml">
  <primary command="/gaia-review-a11y">Review UX design for WCAG 2.1 accessibility compliance</primary>
  <alternative command="/gaia-create-arch" context="If accessibility review will be done later"/>
</next-step>
</workflow>
